# pantheon is a plugin, not an app — nothing to build. Ignore only local cruft.
.pantheon/
learning-inbox.md
__pycache__/
*.pyc
.DS_Store

# transient / runtime state (never part of the plugin)
.omc/
_preview.html
last-route.json
usage-*.json*
effort.json
update-check.json
